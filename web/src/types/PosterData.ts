export type PosterData = {
    timestamp: string
    filename: string
    legend: string
    title: string
    timestart: string
    timeend: string
    place: string
    summary: string
}
  